#include "timemanager.h"

namespace world {

TimeManager::TimeManager(game::GameContext &context) {
    (void) context;
}

void TimeManager::incTimeAround(spatial::UintCoord coord, float time) {
    (void) coord;
    (void) time;
}

}
